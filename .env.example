# Development (local port 5009)
VITE_API_BASE_URL=http://localhost:5009

# Production (leave empty for relative URLs through Nginx reverse proxy, or use full domain)
# VITE_API_BASE_URL=https://portal.gemshine.tech
